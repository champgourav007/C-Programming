#include<stdio.h>
#include<math.h>
main()
{
    int  a,b,f,x;
    printf("Enter Value of b");
    scanf("%d",&b);
    if (b>=2)
    {
        f=a*pow(x,2)+b;

        printf("The value is %d",f);
    }
    else if (b<2)
    {
        f=a*x+b*x;
        printf("The value is %d");
    }

}
